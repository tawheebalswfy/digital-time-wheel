# DIGITAL TIME WHEEL XAU — implementation plan

1. Inspect primary video across its full duration, extract timed visuals and transcribe audio if locally available. Preserve source hash and coverage limitations. Secondary screenshots have not been separately supplied.
2. Record confirmed evidence, inferences and hypotheses before engine implementation. Document mathematical definitions, decimal policies, timezone and nonpredictive assumptions.
3. Build a local Python/FastAPI + React/TypeScript application with SQLite, a real MT5 adapter, strict CSV ingestion and a clearly disconnected initial state.
4. Implement selectable time/price models, digital gates, market indicators and delayed-confirmed swings, auditable component scores and generated targets.
5. Render the interactive 36-sector wheel, candlestick chart, multi-timeframe analysis, settings, immutable signals and experiment comparison.
6. Implement causal backtests with next-bar execution, spread/slippage, conservative intrabar outcomes, walk-forward splits, out-of-sample statistics and forward horizon evaluation.
7. Test mathematics, causality, persistence, adapters, API and WebSocket. Build the frontend, run startup checks and provide PowerShell instructions. Distinguish tests on constructed fixtures from validation on actual gold data.

## Acceptance constraints

No proprietary formula reconstruction claim; no trading orders; no fabricated live prices or historical edge; no hidden target constant; original signal payloads immutable. MT5 requires an installed terminal and an authenticated broker session. Historical performance requires actual timestamped market bars. Missing external prerequisites must be reported, never simulated as successful connections.
